[tool.poetry]
name = "rda-toolbox"
version = "0.1.0"
description = ""
authors = ["timoleistner <leistner.timo@googlemail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
