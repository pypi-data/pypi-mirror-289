title: BuildGrid CAS Service configuration
type: object
propertyNames:
  pattern: "^[A-Za-z0-9-]*$"
properties:
  storage:
    type: storage
  read-only:
    type: boolean
  tree-cache-size:
    type: number
  tree-cache-ttl-minutes:
    type: number
required:
  - storage
