[tool.poetry]
name = "practice-poetry-project-release"
version = "0.2.2"  # poetry-dynamic-versioningによって書き換わるのでこれでOK.
description = ""
authors = ["nakayamaryo0731 <ryo_nakayama@caddi.jp>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.0.3"
flask-sqlalchemy = "^3.1.1"
flask-cors = "^4.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry-dynamic-versioning]
enable = false
style = "pep440"