def model_data(data):
    """
    Build and train a model on the given dataset.

    Args:
        data (list): List of data entries for modeling.

    Returns:
        object: Trained model object.
    """
    # Example data modeling
    model = {
        'trained': True,
        'data_size': len(data),
    }
    return model
