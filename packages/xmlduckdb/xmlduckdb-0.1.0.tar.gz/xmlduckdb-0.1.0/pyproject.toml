[tool.poetry]
name = "xmlduckdb"
version = "0.1.0"
description = "A package for processing XML with DuckDB."
authors = ["Isaac Moore <isaaacmooreuky@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"
duckdb = "^1.0.0"
lxml = "^5.3.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
