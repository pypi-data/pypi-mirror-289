run pip install dj-waanverse-auth

configure restframework according to the docs 

to install the package
run migrtions

add urls to the urls file

add the custom backends

add the default authentication in restframework

ensure the templates are configured as well.
REST_FRAMEWORK = {
    ...
    "DEFAULT_AUTHENTICATION_CLASSES": ("dj_waanverse_auth.backends.JWTAuthentication",),
    ...
}


set up the simplejwt settings minly the life times



SIMPLE_JWT = {
    "ACCESS_TOKEN_LIFETIME": timedelta(minutes=110),
    "REFRESH_TOKEN_LIFETIME": timedelta(days=1),
    ...
}


add the middleware 

    "dj_waanverse_auth.middleware.CookiesHandlerMiddleware",



configure the smtp settings in the settings.py



running tests 
coverage run demo/manage.py test -v 2 && coverage report && coverage html