[project]
name = "grect"
version = "0.0.1"
authors = [
  { name = "Houqi Li", email = "lihouqi007@gmail.com"},
]
dependencies = []
description = "Generate a rectangle through a certain point and angle, with adjustable width and length."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[build-system]
requires = ["setuptools>=60.9.3"]
build-backend = "setuptools.build_meta"
#
[project.urls]
"Homepage" = "https://github.com/tanxinyue"
"Bug Tracker" = "https://github.com/tanxinyue/issues"
