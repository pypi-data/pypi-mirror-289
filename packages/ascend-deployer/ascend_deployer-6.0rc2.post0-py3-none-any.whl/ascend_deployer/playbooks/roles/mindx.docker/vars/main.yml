# defaults file for ascend.docker
docker_pkg_dir: "{{ resource_dir }}/docker/{{ os_package_name }}"
tool_dir: "{{ resource_dir }}/tool"
nerdctl_pkg_name: "{{ 'nerdctl-1.4.0-linux-amd64.tar.gz' if ansible_architecture == 'x86_64' else 'nerdctl-1.4.0-linux-arm64.tar.gz' }}"