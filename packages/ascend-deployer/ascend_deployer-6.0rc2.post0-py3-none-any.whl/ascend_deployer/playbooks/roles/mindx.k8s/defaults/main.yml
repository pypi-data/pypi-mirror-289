# defaults file for mindx.k8s.master

calico_version_3_20: v3.20.2
calico_version_3_25: v3.25.0

CALICO_YAML_DIR: "/root/calico_yaml"
ORIGIN_KUBE_VIP_IMAGE: ghcr.io/kube-vip/kube-vip
KUBE_VIP_IMAGE: "{{ 'ghcr.io/kube-vip' if use_old_k8s_version else ('registry.k8s.io/kube-vip' if use_harbor else 'registry.k8s.io/plndr/kube-vip') }}"
KVVERSION: "{{ 'v0.4.4' if use_old_k8s_version else 'latest' }}"
