[tool.poetry]
name = "pyvalidify"
version = "1.0.1"
description = "Python library for automated and manual runtime data type validation."
authors = ["Patryk Jesionka <jesionka.patryk13@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
parameterized = "^0.9.0"
black = "^24.8.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
