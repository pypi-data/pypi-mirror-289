from setuptools import setup, find_packages

setup(
	name='tspoon',
	version='0.1',
	packages=find_packages(),
	install_requires=[
		# 'os',
		# 'time',
		# 'sys',
		# 'pickle',
		# 'pandas',
		# 'numpy',
		# 'regex',
		# 'datetime',
		# 'matplotlib',
		# 'statsmodels',
		# 'copy'
	]
)