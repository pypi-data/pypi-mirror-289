[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0"]
[project]
name = "my-package-sdkwewqe"
description = "test"
version = "4.5.5"
readme = "README.md"
requires-python = ">=3.7"
keywords = []
authors = [{name = "Zahra", email = "zahra.naseem@apimatic.io"}]
urls = {Documentation = "https://apimatic.io"}
dependencies = ["apimatic-core~=0.2.0, >= 0.2.15", "apimatic-core-interfaces~=0.1.0, >= 0.1.5", "apimatic-requests-client-adapter~=0.1.0, >= 0.1.6", "python-dateutil~=2.8.1", "jsonschema~=3.2.0"]
classifiers = []
[project.optional-dependencies]
testutils = ["pytest>=7.2.2"]
[tool.setuptools.packages.find]
exclude = ["tests", "tests.*"]
