# -*- coding: utf-8 -*-

"""
lob

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from lob.api_helper import APIHelper
from lob.models.datum import Datum


class UploadsReportResponse(object):

    """Implementation of the 'UploadsReportResponse' model.

    TODO: type model description here.

    Attributes:
        data (List[Datum]): TODO: type description here.
        next_url (str): Url of next page of items in list.
        prev_url (str): Url of previous page of items in list.
        count (int): number of resources in a set
        total_count (int): Indicates the total number of records. Provided
            when the request specifies an "include" query parameter

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "data": 'data',
        "count": 'count',
        "total_count": 'total_count',
        "next_url": 'next_url',
        "prev_url": 'prev_url'
    }

    _optionals = [
        'next_url',
        'prev_url',
    ]

    _nullables = [
        'next_url',
        'prev_url',
    ]

    def __init__(self,
                 data=None,
                 count=None,
                 total_count=None,
                 next_url=APIHelper.SKIP,
                 prev_url=APIHelper.SKIP):
        """Constructor for the UploadsReportResponse class"""

        # Initialize members of the class
        self.data = data 
        if next_url is not APIHelper.SKIP:
            self.next_url = next_url 
        if prev_url is not APIHelper.SKIP:
            self.prev_url = prev_url 
        self.count = count 
        self.total_count = total_count 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if dictionary is None:
            return None

        # Extract variables from the dictionary
        data = None
        if dictionary.get('data') is not None:
            data = [Datum.from_dictionary(x) for x in dictionary.get('data')]
        count = dictionary.get("count") if dictionary.get("count") else None
        total_count = dictionary.get("total_count") if dictionary.get("total_count") else None
        next_url = dictionary.get("next_url") if "next_url" in dictionary.keys() else APIHelper.SKIP
        prev_url = dictionary.get("prev_url") if "prev_url" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(data,
                   count,
                   total_count,
                   next_url,
                   prev_url)
