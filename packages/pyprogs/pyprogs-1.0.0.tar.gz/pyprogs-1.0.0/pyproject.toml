[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "pyprogs"
version = "1.0.0"
description = "pyprogs"
authors = ["Ananth G S <ananth.gouri@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^6.2"

[tool.poetry.scripts]
my-script = "pyprogs:main"
