[project]
version = "0.3.0"
name = "lumberkid-rs"

[build-system]
requires = ["maturin>=1.2,<2.0"]
build-backend = "maturin"

[tool]
rye = { dev-dependencies = ["python-semantic-release>=9.8.6"] }

[tool.maturin]
bindings = "bin"

[tool.semantic_release]
branch = "main"
version_toml = ["pyproject.toml:project.version"]
